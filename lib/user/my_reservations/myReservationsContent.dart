import "package:flutter/material.dart";

class MyReservationsContent extends StatelessWidget {
  const MyReservationsContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}